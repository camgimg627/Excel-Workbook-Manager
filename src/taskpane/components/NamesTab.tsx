import * as React from "react";
import { Text } from "@fluentui/react-components";

const NamesTab: React.FC = () => {
  return <Text>Manage named ranges and workbook names here.</Text>;
};

export default NamesTab;
