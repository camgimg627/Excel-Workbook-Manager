import * as React from "react";
import { Text } from "@fluentui/react-components";

const FormattingTab: React.FC = () => {
  return <Text>Apply and adjust formatting options here.</Text>;
};

export default FormattingTab;
