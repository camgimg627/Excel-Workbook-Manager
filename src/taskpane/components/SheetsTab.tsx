import * as React from "react";
import { Text } from "@fluentui/react-components";

const SheetsTab: React.FC = () => {
  return <Text>Work with worksheets and sheet-level operations here.</Text>;
};

export default SheetsTab;
